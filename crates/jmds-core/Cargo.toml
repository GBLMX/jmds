[package]
name = "jmds-core"
version = "0.1.0"
edition = "2024"
description = "Just My DeepSeek: the engine — sessions, panes, configuration, the event bus"
license = "MIT"

[dependencies]
color-eyre = { workspace = true }
jmds-api = { path = "../jmds-api" }
dirs = { workspace = true }
globset = { workspace = true }
log = { workspace = true }
notify = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
toml_edit = { workspace = true }
tracing = { workspace = true }
tracing-appender = { workspace = true }
tracing-subscriber = { workspace = true }

# The process-group kill in `tools::bash`: killing `bash` alone leaves what it started running.
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }
